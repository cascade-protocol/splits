/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { SQUADS_SMART_ACCOUNT_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const EXTEND_TRANSACTION_BUFFER_DISCRIMINATOR = new Uint8Array([
  190, 86, 246, 95, 231, 154, 229, 91,
]);

export function getExtendTransactionBufferDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXTEND_TRANSACTION_BUFFER_DISCRIMINATOR,
  );
}

export type ExtendTransactionBufferInstruction<
  TProgram extends string = typeof SQUADS_SMART_ACCOUNT_PROGRAM_PROGRAM_ADDRESS,
  TAccountSettings extends string | AccountMeta<string> = string,
  TAccountTransactionBuffer extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSettings extends string
        ? ReadonlyAccount<TAccountSettings>
        : TAccountSettings,
      TAccountTransactionBuffer extends string
        ? WritableAccount<TAccountTransactionBuffer>
        : TAccountTransactionBuffer,
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      ...TRemainingAccounts,
    ]
  >;

export type ExtendTransactionBufferInstructionData = {
  discriminator: ReadonlyUint8Array;
  buffer: ReadonlyUint8Array;
};

export type ExtendTransactionBufferInstructionDataArgs = {
  buffer: ReadonlyUint8Array;
};

export function getExtendTransactionBufferInstructionDataEncoder(): Encoder<ExtendTransactionBufferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["buffer", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: EXTEND_TRANSACTION_BUFFER_DISCRIMINATOR,
    }),
  );
}

export function getExtendTransactionBufferInstructionDataDecoder(): Decoder<ExtendTransactionBufferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["buffer", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getExtendTransactionBufferInstructionDataCodec(): Codec<
  ExtendTransactionBufferInstructionDataArgs,
  ExtendTransactionBufferInstructionData
> {
  return combineCodec(
    getExtendTransactionBufferInstructionDataEncoder(),
    getExtendTransactionBufferInstructionDataDecoder(),
  );
}

export type ExtendTransactionBufferInput<
  TAccountSettings extends string = string,
  TAccountTransactionBuffer extends string = string,
  TAccountCreator extends string = string,
> = {
  settings: Address<TAccountSettings>;
  transactionBuffer: Address<TAccountTransactionBuffer>;
  /** The signer on the smart account that created the TransactionBuffer. */
  creator: TransactionSigner<TAccountCreator>;
  buffer: ExtendTransactionBufferInstructionDataArgs["buffer"];
};

export function getExtendTransactionBufferInstruction<
  TAccountSettings extends string,
  TAccountTransactionBuffer extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address =
    typeof SQUADS_SMART_ACCOUNT_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ExtendTransactionBufferInput<
    TAccountSettings,
    TAccountTransactionBuffer,
    TAccountCreator
  >,
  config?: { programAddress?: TProgramAddress },
): ExtendTransactionBufferInstruction<
  TProgramAddress,
  TAccountSettings,
  TAccountTransactionBuffer,
  TAccountCreator
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SQUADS_SMART_ACCOUNT_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    settings: { value: input.settings ?? null, isWritable: false },
    transactionBuffer: {
      value: input.transactionBuffer ?? null,
      isWritable: true,
    },
    creator: { value: input.creator ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.settings),
      getAccountMeta(accounts.transactionBuffer),
      getAccountMeta(accounts.creator),
    ],
    data: getExtendTransactionBufferInstructionDataEncoder().encode(
      args as ExtendTransactionBufferInstructionDataArgs,
    ),
    programAddress,
  } as ExtendTransactionBufferInstruction<
    TProgramAddress,
    TAccountSettings,
    TAccountTransactionBuffer,
    TAccountCreator
  >);
}

export type ParsedExtendTransactionBufferInstruction<
  TProgram extends string = typeof SQUADS_SMART_ACCOUNT_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    settings: TAccountMetas[0];
    transactionBuffer: TAccountMetas[1];
    /** The signer on the smart account that created the TransactionBuffer. */
    creator: TAccountMetas[2];
  };
  data: ExtendTransactionBufferInstructionData;
};

export function parseExtendTransactionBufferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExtendTransactionBufferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      settings: getNextAccount(),
      transactionBuffer: getNextAccount(),
      creator: getNextAccount(),
    },
    data: getExtendTransactionBufferInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
